I"�<p>Ran into another problem today with Xcode while updating <a href="/apps">Snow Music</a> for iPhone 5’s and found the solution <a href="http://createdigitalnoise.com/discussion/1603/libpd-linking-problems-on-xcode-4-5/p1">on CDN</a>. Turns out, Xcode 4.5 is causing all kinds of problems with linking static libraries, and the default build settings don’t work with <a href="http://libpd.cc">libpd</a>.</p>

<p>The build fails with an error like:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Undefined symbols for architecture armv7s:
</code></pre></div></div>

<p>And the solution is to go into Build Settings for the main application’s target and change “Build Active Architecture Only” to YES in the Debug configuration (This matches how libpd is set up).</p>

<p>Check out <a href="http://createdigitalnoise.com/discussion/1603/libpd-linking-problems-on-xcode-4-5/p1">reakinator’s explanation</a>.</p>
:ET